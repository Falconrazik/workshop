import { StyleSheet, Text, SafeAreaView, View } from 'react-native';

export default function createRate (props) {
    return (
        <SafeAreaView>
            <Text>INTERESTS</Text>
        </SafeAreaView>
    )
};
