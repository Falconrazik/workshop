import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

export default function Dashboard (props) {
    return (
        <SafeAreaView>
            <Text>DASHBOARD</Text>
        </SafeAreaView>
    )
};
