import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

export default function Landing (props) {
    return (
        <SafeAreaView>
            <Text>LANDING</Text>
        </SafeAreaView>
    )
};
